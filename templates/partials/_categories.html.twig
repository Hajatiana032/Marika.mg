{% for category in categories %}
    <div class="col-sm-6 col-md-4 col-lg-4">
        <a class="dropdown-item {% if category.slug == current_category %}active{% endif %}"
           href="{{ path('app_category', {slug: category.slug}) }}">
            {{ category.name }}
        </a>
    </div>
{% endfor %}
