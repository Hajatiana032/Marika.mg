<div class="container bg-white">
    <nav class="navbar navbar-expand-lg bg-white sticky-top">
        <div class="container">
            <a href="{{ path('app_home') }}" class="navbar-brand d-flex align-items-center">
                <img src="{{ asset('img/logo.svg') }}" alt="logo" width="30px" height="30px">
                Marika.mg
            </a>
            <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarToggler"
                    aria-controls="navbarToggler"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarToggler">
                <ul class="navbar-nav m-auto">
                    <li class="nav-item">
                        <a href="{{ path('app_home') }}" class="nav-link {% if current_menu is defined and current_menu
                            == 'home' %}active{% endif %}">Acceuil</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_shop') }}" class="nav-link {% if current_menu is defined and current_menu
                            == 'shop' %}active{% endif %}">Boutique</a>
                    </li>
                    <li class="nav-item dropdown menu-area">
                        <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown"
                           aria-expanded="false" id="navbarDropdown">Catégories</a>
                        <div class="dropdown-menu mega-menu border-0 border-top border-blue-600 border-5"
                             aria-labelledby="navbarDropdown">
                            <div class="container">
                                <div class="row">
                                    {{ render(controller('App\\Controller\\CategoryController::categoriesList')) }}
                                </div>
                            </div>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">A-propos</a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">Contact</a>
                    </li>
                </ul>
                <div class="d-flex">
                    <a href="#" class="link-blue-700 me-3 fw-semibold text-decoration-none">Inscription</a>
                    <a href="#" class="link-blue-700 fw-semibold text-decoration-none">Connexion</a>
                </div>
            </div>
        </div>
    </nav>
</div>
