<div class="col-lg-6">
    <div class="card border-emerald-500 shadow border-2 h-100">
        <div class="card-body">
            <div class="card-title text-emerald-500">
                <i class="fa {{ icon }} fa-3x"></i>
            </div>
            <h2 class="fw-semibold">{{ title }}</h2>
            <p class="text-dark">{{ content }}</p>
        </div>
    </div>
</div>
