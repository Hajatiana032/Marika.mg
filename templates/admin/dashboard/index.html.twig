{% extends 'admin/admin.html.twig' %}

{% block admin_title %}
    Tableau de bord
{% endblock %}
{% block main %}
    <div class="container-fluid">
        <h1>Tableau de bord</h1>
        <div class="row gy-5">
            <div class="col-xl-6">
                {{ render_chart(chart_category) }}
            </div>
            <div class="col-xl-6">
                {{ render_chart(chart_brand) }}
            </div>
            <div class="col-xl-6">
                {{ render_chart(chart) }}
            </div>
        </div>
    </div>
{% endblock %}
